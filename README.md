Modern Full-Stack Admin Panel:​ A robust backend built with Python FastAPI (SQLAlchemy, MySQL) and a responsive frontend using React (TypeScript/Vite). Features JWT authentication, role-based access control (RBAC), and RESTful APIs for efficient data management. Ideal for rapid development of enterprise-level admin systems.
myenv\Scripts\activate
uvicorn app.main:app --reload